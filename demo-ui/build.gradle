plugins {
	id "com.moowork.node" version "1.3.1"
}

task npmBuild(type: NpmTask) {
	mustRunAfter(npmInstall)
	args = ["run", "build"]
}

task npmPackage(dependsOn: 'npmInstall' ) {
	dependsOn(npmBuild)
}

task build( dependsOn: 'npmPackage' ) {}